@model Publisher

<div class="row">
    <div class="col-2">@Model.Name</div>
    <div class="col-6">
    @foreach(Book book in Model.Books)
            {
                <p>@book.Title</p>
            }
    </div>
</div>